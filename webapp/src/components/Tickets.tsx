import React from 'react';
import { Title, FlexCenter } from './core';

const Tickets: React.FC = () => {
  return (
    <FlexCenter>
        <Title>Tickets</Title>
    </FlexCenter>
  )
}

export default Tickets;